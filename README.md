# WonderTrackAcademy

## Run locally

```bash
npm install
npm start
```

Open `http://localhost:3000` to view the site. Join form submissions are stored in `data/join-us.jsonl` for easy automation later.
